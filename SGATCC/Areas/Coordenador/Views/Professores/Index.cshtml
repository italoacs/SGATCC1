@model IEnumerable<SGATCC.Areas.Coordenador.Models.Professor>

@{
    ViewData["Title"] = "Lista de Professores";
}

<h1>Lista de Professores</h1>

<p>
    <a asp-action="Create" class="btn btn-success">Cadastrar novo Professor</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cpf)
            </th>
            <th>
                Data de Nascimento
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sexo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Telefone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disciplina.Descricao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cpf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataNascimento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sexo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Telefone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Disciplina.Descricao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProfessorId" class="btn btn-success btn-sm">Alterar</a> &nbsp;
                <a asp-action="Details" asp-route-id="@item.ProfessorId" class="btn btn-primary btn-sm">Detalhes</a> &nbsp;
                <a asp-action="Delete" asp-route-id="@item.ProfessorId" class="btn btn-danger btn-sm">Excluir</a>
            </td>
        </tr>
        }
    </tbody>
</table>
